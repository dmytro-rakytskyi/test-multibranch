pipeline {
  agent any

  stages {
    stage("build") {
      steps {
        echo 'build my FEATURE branch'
        echo 'another change featured '
        echo 'another change featured AFter Jenkins was moved to Git Branch SOurce(instead of GitHub) and Feature branches are not matched anymore'
      }
    }
  }
}
